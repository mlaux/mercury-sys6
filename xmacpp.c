#include <MacTCP.h>#include <stdio.h>#include <stdlib.h>#include "tcp.h"int main(void){	long google_ip;	StreamPtr my_stream;	OSErr connect_err;		printf("Hello!\n");	tcp_init();	printf("Called tcp_init\n");	dns_init(NULL);	printf("called dns_init\n");	google_ip = dns_lookup("google.com");	printf("googles ip is %s\n", dns_format_ip(google_ip));		my_stream = tcp_create_stream();	printf("got stream %d\n", my_stream);		connect_err = tcp_connect(my_stream, google_ip, 80);	printf("connected - osErr was %d\n", connect_err);		tcp_release_stream(my_stream);	printf("released\n");	tcp_cleanup();	dns_cleanup();	printf("called dns_cleanup\n");	return 0;}